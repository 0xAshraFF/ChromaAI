services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  demo:
    build: .
    ports:
      - "8501:8501"
    command: streamlit run demo/app.py --server.port=8501 --server.address=0.0.0.0
    restart: unless-stopped
